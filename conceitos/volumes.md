# O que são volumes?

- volumes são um espaço de armazenamento fora do container, usado para guardar dados de forma persistente;

- por padrão os dados de um container somem, mas os volumes conseguem persistir os dados no container;

# Criar volume

- docker volume create nome-volume
